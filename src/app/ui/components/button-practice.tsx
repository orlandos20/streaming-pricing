'use client';

import { Button } from '@/components/ui/button';

export function ButtonPractive() {
  return (
    <Button onClick={() => console.log('Button clicked!')}>
      ShadCN Button
    </Button>
  );
}
